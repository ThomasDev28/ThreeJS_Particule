{
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview"
  },
  "devDependencies": {
    "gh-pages": "^3.2.0",
    "vite": "^2.3.5",
    "predeploy": "npm run build && cp -r public dist/public",
    "deploy": "gh-pages -d dist",
  },
  "dependencies": {
    "three": "^0.129.0"
  }
}
